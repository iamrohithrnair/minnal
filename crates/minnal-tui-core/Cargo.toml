[package]
name = "minnal-tui-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crossterm = "0.29"
serde = { version = "1", features = ["derive"] }
minnal-memory-types = { path = "../minnal-memory-types" }
