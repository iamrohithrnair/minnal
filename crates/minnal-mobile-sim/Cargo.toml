[package]
name = "minnal-mobile-sim"
version = "0.1.0"
edition = "2024"
description = "Headless-first mobile simulator and control CLI for minnal"

[dependencies]
anyhow = "1"
bytemuck = { version = "1", features = ["derive"] }
clap = { version = "4", features = ["derive"] }
minnal-mobile-core = { path = "../minnal-mobile-core" }
libc = "0.2"
pollster = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
wgpu = "0.19"
winit = "0.29"

[dev-dependencies]
tempfile = "3"
