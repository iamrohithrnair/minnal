[package]
name = "minnal-memory-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
minnal-core = { path = "../minnal-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
