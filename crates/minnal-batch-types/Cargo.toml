[package]
name = "minnal-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
minnal-message-types = { path = "../minnal-message-types" }
serde = { version = "1", features = ["derive"] }
